// import {format} from "date-fns"

// type Format = "YYYY-MM-DD";

// export const useDateFormat = () => {
//   const dateFormat = (src: string, f: Format) => {
//     return format(new Date(src), f);
//   };

//   return {
//     dateFormat,
//   };
// }
